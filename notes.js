const mainEntry = () => {
    const main = {
        data() {
            return {
                data: data
            }
        }
    }
    Vue.createApp(main).mount('#app');
}

mainEntry();

//export { mainEntry };